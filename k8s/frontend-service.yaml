apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: default
spec:
  selector:
    app: rapp-tycoon-frontend
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
  type: NodePort
