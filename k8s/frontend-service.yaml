apiVersion: v1
kind: Service
metadata:
  name: frontend
  labels:
    app: rapp-tycoon-frontend
spec:
  type: LoadBalancer
  selector:
    app: rapp-tycoon-frontend
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
